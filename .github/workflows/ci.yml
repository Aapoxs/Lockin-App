name: CI

on:
  pull_request:
  push:
    branches: [master]

jobs:
  verify:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm audit --omit=dev
      - run: npm run build
      - run: npm test
